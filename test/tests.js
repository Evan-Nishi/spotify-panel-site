import { clearCookie, setCookie } from '../src/utils/cookies'
import {  } from '../src/pages/api/authreq'
describe('cookie handler', () => {
    it.todo('clears cookie')
    it.todo('sets cookie')
})

describe('authorization', () =>{
    it.todo('sets correct cookie')
    it.todo('redirects to correct uri')
})

describe() 